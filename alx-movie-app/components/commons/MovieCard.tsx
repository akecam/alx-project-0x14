import * as React from "react";

interface MovieCardProps {
  propName: type;
}

const MovieCard: React.FC<MovieCardProps> = ({ propName }) => {
  return <div>MovieCard</div>;
};

export default MovieCard;
